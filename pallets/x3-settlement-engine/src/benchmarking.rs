// Temporary benchmarking stub for `x3-settlement-engine` to satisfy `mod benchmarking;`.
#![allow(dead_code)]

#[cfg(test)]
mod benchmarks {
    #[test]
    fn bench_stub() {
        assert!(true);
    }
}
