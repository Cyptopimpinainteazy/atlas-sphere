[package]
name = "pallet-x3-slash"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
frame-support = { workspace = true }
frame-system = { workspace = true }
sp-runtime = { workspace = true }
sp-core = { workspace = true }
sp-std = { workspace = true }
sp-io = { workspace = true }
codec = { workspace = true, features = ["derive"] }
scale-info = { workspace = true, features = ["derive"] }
log = { workspace = true }

# X3 crates
x3-slash = { path = "../../crates/x3-slash", default-features = false }
x3-proof = { path = "../../crates/x3-proof", default-features = false }

# Staking integration
pallet-staking = { git = "https://github.com/paritytech/substrate", rev = "948fbd2", default-features = false }

# Benchmarking
frame-benchmarking = { workspace = true, optional = true }

[features]
default = ["std"]
std = [
    "frame-support/std",
    "frame-system/std",
    "sp-runtime/std",
    "sp-core/std",
    "sp-std/std",
    "sp-io/std",
    "codec/std",
    "scale-info/std",
    "log/std",
    "x3-slash/std",
    "x3-proof/std",
    "pallet-staking/std",
]
runtime-benchmarks = [
    "frame-benchmarking",
    "frame-support/runtime-benchmarks",
    "frame-system/runtime-benchmarks",
    "sp-runtime/runtime-benchmarks",
    "pallet-staking/runtime-benchmarks",
]
try-runtime = [
    "frame-support/try-runtime",
    "frame-system/try-runtime",
    "pallet-staking/try-runtime",
]
