/** @type {import('next').NextConfig} */
const nextConfig = {
  // App directory is now stable in Next.js 14, no experimental config needed
  // Enable src directory structure
  experimental: {
    // App directory lives in src/
  },
};

module.exports = nextConfig;
