[workspace]
members = [
    "programs/basic",
    "programs/x3vm-executor",
    # "programs/FlashloanReceiver", # Disabled: solend-program repo unavailable
    # "programs/ArbEngine", # Disabled: depends on FlashloanReceiver
]
resolver = "2"

[profile.release]
overflow-checks = true
lto = "fat"
codegen-units = 1
[profile.release.build-override]
opt-level = 3
incremental = false
codegen-units = 1
