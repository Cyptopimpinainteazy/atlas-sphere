[build]
# Enable incremental compilation
incremental = true

# Configure WASM target for runtime builds
[target.wasm32-unknown-unknown]
rustflags = ["-C", "link-arg=--no-entry"]

# Configure for getrandom WASM support
[env]
CARGO_CFG_GETRANDOM_JS_ENABLED = "true"
