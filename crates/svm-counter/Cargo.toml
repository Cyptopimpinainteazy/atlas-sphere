[package]
name = "svm_counter"
version = "0.1.0"
edition = "2021"
description = "Simple Solana SVM counter program used for integration tests"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
solana-program = "1.14.19"
