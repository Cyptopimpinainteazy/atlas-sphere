[package]
name = "x3-fees"
version = "0.1.0"
edition = "2021"
description = "X3 Fee Curve Engine — vector-based dynamic fee calculation"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
thiserror = "1.0"

[dev-dependencies]
serde_json = "1.0"
