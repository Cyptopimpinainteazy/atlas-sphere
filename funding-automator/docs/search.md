Google search helpers and safe usage

This project provides a small helper to generate search queries (dorks) that can be used
with compliant search APIs, Google Custom Search, or a manual browsing process.

Important: do not automate scraping of Google search results in ways that violate terms of service.
When integrating search at scale, use official APIs (Google Custom Search API, SerpAPI, etc.)
and respect rate limits.
