version: '3.8'

services:
  secrets-mcp:
    image: vault:1.13.0
    container_name: atlas-secrets-mcp
    ports:
      - "8200:8200"
    environment:
      VAULT_DEV_ROOT_TOKEN_ID: "atlas-dev-token"
      VAULT_DEV_LISTEN_ADDRESS: "0.0.0.0:8200"
    volumes:
      - secrets_data:/vault/data
    healthcheck:
      test: ["CMD", "vault", "status"]
      interval: 10s
      timeout: 3s
      retries: 5

  fs-mcp:
    image: atlas/mcp-filesystem:latest
    container_name: atlas-fs-mcp
    ports:
      - "8080:8080"
    environment:
      REPO_ROOT: "/workspace"
      MAX_FILE_SIZE_MB: "32"
    volumes:
      - ../:/workspace
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 10s
      timeout: 3s
    depends_on:
      secrets-mcp:
        condition: service_healthy

  git-mcp:
    image: atlas/mcp-git:latest
    container_name: atlas-git-mcp
    ports:
      - "9418:9418"
      - "8081:8081"
    environment:
      GIT_USER: "atlas-bot"
    volumes:
      - ../:/repo
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8081/status"]
      interval: 10s
      timeout: 3s
    depends_on:
      fs-mcp:
        condition: service_healthy

  docker-mcp:
    image: atlas/mcp-docker:latest
    container_name: atlas-docker-mcp
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    healthcheck:
      test: ["CMD", "docker", "info"]
      interval: 10s
      timeout: 3s
    depends_on:
      fs-mcp:
        condition: service_healthy

  exec-mcp:
    image: atlas/mcp-exec:latest
    container_name: atlas-exec-mcp
    ports:
      - "8443:8443"
    environment:
      TIME_LIMIT_SEC: "30"
      MEM_LIMIT_MB: "512"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8443/v1/ping"]
      interval: 5s
      timeout: 2s
    depends_on:
      fs-mcp:
        condition: service_healthy

  db-mcp:
    image: postgres:15
    container_name: atlas-db-mcp
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: "atlas"
      POSTGRES_USER: "atlas"
      POSTGRES_PASSWORD: "atlas_password"
    volumes:
      - db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U atlas"]
      interval: 10s
      timeout: 5s
      retries: 5

  atlas-node-mcp:
    image: atlas/atlas-sphere-node:dev
    container_name: atlas-node-mcp
    ports:
      - "9933:9933"
      - "9944:9944"
    environment:
      NODE_ENV: "dev"
    volumes:
      - node_data:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9933/health"]
      interval: 10s
      timeout: 3s
    depends_on:
      secrets-mcp:
        condition: service_healthy
      db-mcp:
        condition: service_healthy

  rpc-router-mcp:
    image: atlas/mcp-rpc-router:latest
    container_name: atlas-rpc-router-mcp
    ports:
      - "8060:8060"
    environment:
      ROUTE_POLICY: "latency-first"
      ETHEREUM_MAINNET_RPC: "https://eth-mainnet.public.blastapi.io"
      POLYGON_MAINNET_RPC: "https://polygon-rpc.com"
    depends_on:
      atlas-node-mcp:
        condition: service_healthy

  web3-evm-mcp:
    image: atlas/mcp-web3-evm:latest
    container_name: atlas-web3-evm-mcp
    ports:
      - "8545:8545"
    environment:
      DEFAULT_RPC: "http://localhost:8545"
    volumes:
      - ../:/workspace
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8545/rpc/health"]
      interval: 5s
      timeout: 2s
    depends_on:
      exec-mcp:
        condition: service_healthy
      fs-mcp:
        condition: service_healthy
      docker-mcp:
        condition: service_healthy
      git-mcp:
        condition: service_healthy
      rpc-router-mcp:
        condition: service_healthy

  svm-mcp:
    image: atlas/mcp-svm:latest
    container_name: atlas-svm-mcp
    ports:
      - "8899:8899"
    volumes:
      - ../:/workspace
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8899/health"]
      interval: 10s
      timeout: 3s
    depends_on:
      exec-mcp:
        condition: service_healthy
      fs-mcp:
        condition: service_healthy
      git-mcp:
        condition: service_healthy
      rpc-router-mcp:
        condition: service_healthy

  wallet-mcp:
    image: atlas/mcp-wallet:latest
    container_name: atlas-wallet-mcp
    ports:
      - "8444:8444"
    environment:
      WALLET_API_ALLOWLIST: "127.0.0.1"
    depends_on:
      fs-mcp:
        condition: service_healthy
      git-mcp:
        condition: service_healthy

  explorer-mcp:
    image: atlas/mcp-explorer:latest
    container_name: atlas-explorer-mcp
    ports:
      - "4000:4000"
    depends_on:
      rpc-router-mcp:
        condition: service_healthy
      db-mcp:
        condition: service_healthy

  logging-mcp:
    image: atlas/mcp-logging:latest
    container_name: atlas-logging-mcp
    ports:
      - "9200:9200"

  monitoring-mcp:
    image: prom/prometheus:latest
    container_name: atlas-monitoring-mcp
    ports:
      - "9090:9090"
    depends_on:
      logging-mcp:
        condition: service_healthy

volumes:
  secrets_data:
  db_data:
  node_data:
