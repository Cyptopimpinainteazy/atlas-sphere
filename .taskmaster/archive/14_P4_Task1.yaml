id: "14"
title: "P4 Day 1: Environment Setup & Verification"
description: "Setup the P4 development environment including Python venv, CUDA verification, and dependencies."
assignee: "ROADIE"
status: "pending"
priority: "high"
dependencies: []
metadata:
  blueprint_task: 1
  phase: 4
  day: 1
  commands:
    - "python3 -m venv .venv-p4"
    - "source .venv-p4/bin/activate"
    - "pip install -q numpy pytest cupy-cuda11x ed25519-donna solders"
    - "python3 --version && nvcc --version && nvidia-smi"
    - "git checkout -b feat/p4-gpu"
