<<<<<<< REPO
<<<<<<< REPO
export type XPost = {
  handle: string
  text: string
}

export async function sendX (post: XPost) {
  // Placeholder: modern X (Twitter) APIs require OAuth and elevated access.
  // Keep this stubbed so integration can be implemented later.
  return {
    ok: true,
    sentAt: new Date().toISOString(),
    post
  }
}

=======
export type XPost = {
  handle: string
  text: string
}

export async function sendX (post: XPost) {
  // Placeholder: modern X (Twitter) APIs require OAuth and elevated access.
  // Keep this stubbed so integration can be implemented later.
  return {
    ok: true,
    sentAt: new Date().toISOString(),
    post
  }
}

>>>>>>> IMPORT (TEXT)

=======
export type XPost = {
  handle: string
  text: string
}

export async function sendX (post: XPost) {
  // Placeholder: modern X (Twitter) APIs require OAuth and elevated access.
  // Keep this stubbed so integration can be implemented later.
  return {
    ok: true,
    sentAt: new Date().toISOString(),
    post
  }
}

>>>>>>> IMPORT (TEXT)
